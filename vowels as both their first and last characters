/* have vowels as both their first and last characters */

SELECT DISTINCT CITY
FROM STATION 
WHERE SUBSTR(CITY, 1,1) in  ('A','E','I','O','U','a', 'e', 'i', 'o', 'u')
AND SUBSTR(CITY, -1) in  ('A','E','I','O','U','a', 'e', 'i', 'o', 'u');
